sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t alphalearn/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true alphalearn/react-test npm test -- --coverage

after_success:
  - docker build -t alphalearn/multi-client ./client
  - docker build -t alphalearn/multi-nginx ./nginx
  - docker build -t alphalearn/multi-server ./server
  - docker build -t alphalearn/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push alphalearn/multi-client
  - docker push alphalearn/multi-nginx
  - docker push alphalearn/multi-server
  - docker push alphalearn/multi-worker
